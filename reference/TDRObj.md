# Construct a TDRObj

Construct a TDRObj

## Usage

``` r
TDRObj(...)
```

## Arguments

- ...:

  Named arguments corresponding to TDRObj slots. Legacy (17-slot) slot
  names are automatically mapped to new 13-slot paths.

## Value

A `TDRObj` object.
