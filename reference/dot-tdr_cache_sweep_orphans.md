# Remove orphaned .rds.tmp files left by interrupted writes

Remove orphaned .rds.tmp files left by interrupted writes

## Usage

``` r
.tdr_cache_sweep_orphans(cache_dir)
```

## Arguments

- cache_dir:

  Cache root directory to sweep.
