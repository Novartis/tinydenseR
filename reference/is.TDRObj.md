# Check if an object is a TDRObj

Check if an object is a TDRObj

## Usage

``` r
is.TDRObj(x)
```

## Arguments

- x:

  An object to test.

## Value

Logical: TRUE if `x` is a TDRObj, FALSE otherwise.
