% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dispatch.R, R/lm.graph.embed.R
\name{recluster.Seurat}
\alias{recluster.Seurat}
\alias{recluster.SingleCellExperiment}
\alias{recluster}
\alias{recluster.TDRObj}
\title{Recluster landmarks}
\usage{
\method{recluster}{Seurat}(x, ...)

\method{recluster}{SingleCellExperiment}(x, ...)

recluster(x, ...)

\method{recluster}{TDRObj}(
  x,
  .cl.resolution.parameter = 0.8,
  .cl.method = "snn",
  .small.size = 3,
  .column.name = NULL,
  .seed = 123,
  .verbose = TRUE,
  ...
)
}
\arguments{
\item{x}{A \code{\linkS4class{TDRObj}}, Seurat, SingleCellExperiment, or HDF5AnnData
(anndataR) object with \code{get.graph()} already run.}

\item{...}{Additional arguments passed to methods.}

\item{.cl.resolution.parameter}{Numeric resolution for Leiden CPM (default 0.8).
Internally scaled by 1e-3. Higher = finer clusters.}

\item{.cl.method}{Character: \code{"snn"} or \code{"fgraph"} (default \code{"snn"}).}

\item{.small.size}{Integer threshold for straggler absorption (default 3).}

\item{.column.name}{Character name for storing the solution (default
\code{paste0("leiden.res.", .cl.resolution.parameter)}). Cannot be \code{"ids"}.}

\item{.seed}{Integer for reproducibility (default 123).}

\item{.verbose}{Logical (default TRUE).}
}
\value{
Updated object with new active clustering and refreshed downstream slots.
}
\description{
Re-runs Leiden community detection with new parameters and refreshes all
clustering-dependent downstream slots (cell-level IDs, composition matrices,
traditional fits). This is the recommended way to explore different
resolutions after the initial \code{get.graph()} call.
}
