% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plotNmfDE}
\alias{plotNmfDE}
\alias{plotNmfDE.TDRObj}
\title{Plot nmfDE Scores (Diagnostic and Component Views)}
\usage{
plotNmfDE(x, ...)

\method{plotNmfDE}{TDRObj}(
  x,
  .coef.col,
  .nmfDE.dim = NULL,
  .embed = "umap",
  .point.size = 1,
  .label.size = 3,
  .panel.size = 2,
  .seed = 123,
  ...
)
}
\arguments{
\item{x}{A \code{\linkS4class{TDRObj}}, Seurat, SingleCellExperiment, or HDF5AnnData
(anndataR) object after \code{get.nmfDE()}.}

\item{...}{Additional arguments passed to methods.}

\item{.coef.col}{Character: coefficient name matching a slot in \code{.tdr.obj$nmfDE}.}

\item{.nmfDE.dim}{Integer or NULL: component to visualize (1-indexed). If NULL,
plots Ak vs Sk diagnostic scatter for all components.}

\item{.embed}{Character: either \code{"umap"} or \code{"pca"}. Default "umap".}

\item{.point.size}{Numeric: point size for scatter plots. Default 1.}

\item{.label.size}{Numeric: label size for diagnostic scatter plots. Default 3. Applies only if .nmfDE.dim is NULL.}

\item{.panel.size}{Numeric: panel size in inches. Default 2.}

\item{.seed}{Integer: random seed for point ordering. Default 123.}
}
\value{
A ggplot2 object (if \code{.nmfDE.dim = NULL}) or a patchwork composition
(if \code{.nmfDE.dim} is integer).
}
\description{
Visualize nmfDE results: either a diagnostic overview of all components
or a detailed component-level view showing UMAP embedding and Y-vs-score
scatter.
}
\details{
When component is NULL, displays a diagnostic overview of all components.

\strong{Component view} (\code{.nmfDE.dim = integer}):
\itemize{
\item Left panel: UMAP embedding colored by nmfDE signed scores (diverging scale)
\item Right panel: Scatter of Y vs signed scores (essential for interpretation)
}

\strong{Diagnostic view} (\code{.nmfDE.dim = NULL}):
\itemize{
\item X-axis: Smoothness (Sk); higher = large-scale graph-smooth structure
\item Y-axis: Y-alignment (Ak); higher = stronger density coupling
\item Color: NMF scaling diagonal d_k (component strength)
\item Labels: Component names (nmfDE1, nmfDE2, ...)
}

\strong{Interpreting the diagnostic plot:}
\itemize{
\item DA-dominated (typical): upper-right, high Ak + high Sk
\item DE-dominated (functional): lower region, moderate/low Ak
\item Strong components: bright color (large d_k), regardless of Ak/Sk
}
Unlike specDE, variance cannot be cleanly partitioned: d_k indicates overall
component strength but not independent variance explained.
}
\examples{
\dontrun{
# After running nmfDE
lm.obj <- get.nmfDE(lm.obj, .coef.col = "Infection")

# Diagnostic overview
plotNmfDE(lm.obj, .coef.col = "Infection", .nmfDE.dim = NULL)

# Visualize first component
plotNmfDE(lm.obj, .coef.col = "Infection", .nmfDE.dim = 1)
}

}
\seealso{
\code{\link{get.nmfDE}} for computing nmfDE, \code{\link{plotNmfDEHeatmap}}
for expression heatmaps
}
