% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache.R
\name{.tdr_cache_write}
\alias{.tdr_cache_write}
\title{Write one cached slot to disk (crash-safe via atomic rename)}
\usage{
.tdr_cache_write(object, cache_dir, slot_name, sample_name, compress = FALSE)
}
\arguments{
\item{object}{R object to serialize.}

\item{cache_dir}{Root cache directory for the current run.}

\item{slot_name}{Character – one of \code{"clustering.ids"},
\code{"celltyping.ids"}, \code{"nearest.landmarks"},
\code{"fuzzy.graph"}.}

\item{sample_name}{Character – sample identifier (used as file stem).}

\item{compress}{Passed to \code{saveRDS}. Default \code{FALSE} for speed.}
}
\value{
A metadata list with path, slot, sample, bytes, and schema_v.
}
\description{
Writes \code{object} to an RDS file inside \code{cache_dir/slot_name/}
using an atomic rename pattern (write to a \code{.rds.tmp} file, then
\code{file.rename()} to the final \code{.rds}).  When the \pkg{filelock}
package is installed, a shared file lock is held during the rename to
prevent concurrent writes from clobbering each other.
}
\keyword{internal}
