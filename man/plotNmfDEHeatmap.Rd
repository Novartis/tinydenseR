% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plotNmfDEHeatmap}
\alias{plotNmfDEHeatmap}
\alias{plotNmfDEHeatmap.TDRObj}
\title{Plot nmfDE Expression Heatmap}
\usage{
plotNmfDEHeatmap(x, ...)

\method{plotNmfDEHeatmap}{TDRObj}(
  x,
  .coef.col,
  .nmfDE.dim = 1,
  .model.name = "default",
  .loadings.type = c("de", "signed", "mass"),
  .n.features = 50,
  .order.by = "dens.contrast",
  .add.annot = NULL,
  .order.decreasing = FALSE,
  .viridis.options.annot = c("cividis", "rocket", "inferno", "mako", "magma"),
  .annot.panel.width = 4,
  .annot.panel.height = 0.15,
  .panel.width = 4,
  .panel.height = 3,
  .feature.font.size = 7,
  .show.landmark.labels = FALSE,
  .label.substr.rm = "",
  ...
)
}
\arguments{
\item{x}{A \code{\linkS4class{TDRObj}}, Seurat, SingleCellExperiment, or HDF5AnnData
(anndataR) object after \code{get.nmfDE()}.}

\item{...}{Additional arguments passed to methods.}

\item{.coef.col}{Character: coefficient name matching a slot in \code{.tdr.obj$nmfDE}.}

\item{.nmfDE.dim}{Integer or integer vector: nmfDE component(s) to use for:
(1) ranking features by absolute loading, and (2) ordering landmarks (if
\code{.order.by = NULL}). If vector (e.g., \code{c(1, 2)}), the first dimension
is used for feature ranking, and landmarks are sorted by all dimensions sequentially.
Default 1.}

\item{.model.name}{Character: name of the fitted model (default "default").}

\item{.loadings.type}{Character: which loadings to use for feature selection.
One of \code{"signed"} (default; d\emph{h+ - d}h- from the NMF factorization),
\code{"de"} (regression of centered X on signed scores; standard DE
interpretation), or \code{"mass"} (regression of nonneg X0 on W).}

\item{.n.features}{Integer: maximum number of features to display. For RNA, features
are ranked by absolute loading and top \code{.n.features} are shown. For cytometry,
all markers are shown and this parameter is ignored. Default 50.}

\item{.order.by}{Controls landmark ordering. One of:
\describe{
\item{\code{"dens.contrast"}}{(default) Order by centered density contrast
from \code{.coef.col}.}
\item{\code{"nmfDE.dim"}}{Order by the selected \code{.nmfDE.dim} signed score(s).}
\item{A numeric matrix with column names}{Landmarks are sorted by columns
sequentially (first column primary, etc.) and each column is displayed as
an annotation strip with the column name as legend title.}
}}

\item{.add.annot}{Numeric matrix with column names: displayed as annotation strips.
These are added as additional annotation strips after the density contrast and before
nmfDE score strips.}

\item{.order.decreasing}{Logical: sort landmarks in decreasing order? Default FALSE.}

\item{.viridis.options.annot}{Character vector: viridis color options for annotation
strips (density contrast + nmfDE dimensions + .order.by columns). Cycled if fewer
options than strips. Default \code{c("cividis", "rocket", "inferno", "mako", "magma")}.}

\item{.annot.panel.width}{Numeric: width of annotation strips in inches. Default 4.}

\item{.annot.panel.height}{Numeric: height of each annotation strip in inches. Default 0.15.}

\item{.panel.width}{Numeric: width of expression heatmap panel in inches. Default 4.}

\item{.panel.height}{Numeric: height of expression heatmap panel in inches. Default 3.}

\item{.feature.font.size}{Numeric: font size for feature labels. Default 7.}

\item{.show.landmark.labels}{Logical: show landmark IDs on x-axis? Default FALSE
(usually too many to display).}

\item{.label.substr.rm}{Character substring to remove from density contrast label (default "").}
}
\value{
A ggplot2 object (or patchwork composition with annotation strips).
}
\description{
Creates an efficient expression heatmap with landmarks as columns and top-loaded
features as rows. Landmarks are ordered by nmfDE signed scores (or custom ordering),
and features are ranked by absolute loading. For RNA data, expression is properly
normalized, log-transformed, and centered.
}
\details{
\strong{Feature selection and ordering}:
Features are \emph{selected} by absolute loading value for the first element of \code{.nmfDE.dim}
(for RNA, the top \code{.n.features}; for cytometry, all markers). However, the selected features
are then \emph{ordered} by signed loading (highest positive at top, most negative at bottom),
making it easy to see which genes are up vs down-regulated along the nmfDE component.

\strong{Loadings types}:
\describe{
\item{\code{"signed"}}{(default) Derived directly from the NMF factorization:
d_k * h+_k - d_k * h-_k. The most natural choice for nmfDE: positive means
gene carries more mass through the positive-contrast block than the negative.
No regression step required.}
\item{\code{"de"}}{Regression of centered X on signed scores. Standard DE
interpretation: positive = upregulated with Y, negative = downregulated.}
\item{\code{"mass"}}{Regression of nonneg X0 on W. Captures genes whose expression
level scales with component mass activation (always positive for nonneg input).}
}

\strong{Expression normalization} (RNA only):
To avoid processing the full expression matrix, the function:
\enumerate{
\item Computes row sums (per-landmark library size) from the entire \code{raw.landmarks}
\item Subsets to top features
\item Applies size factor normalization using pre-computed row sums
\item Log2-transforms: \code{log2(x + 1)}
\item Centers each feature (row) to mean 0
}

\strong{Landmark ordering}:
When \code{.order.by} is multi-dimensional (matrix with >1 column), landmarks are
sorted by the first column, then by the second within ties, etc.

\strong{Annotation strips}:
The following are shown as annotation strips at the top:
\enumerate{
\item Density contrast (centered Y from \code{get.lm})
\item nmfDE signed scores for all dimensions in \code{.nmfDE.dim}
\item If \code{.order.by} is provided: each column of \code{.order.by}, with
column names used as legend titles
}
This ensures the relationship between expression patterns and the nmfDE structure
is always visible, plus any custom ordering variables.

\strong{Visualization}:
Uses \code{ggplot2::geom_raster()} for efficient rendering of large heatmaps.
Expression uses a diverging color scale centered at 0. Annotation strips use
sequential viridis scales.
}
\examples{
\dontrun{
# After running nmfDE
lm.obj <- get.nmfDE(lm.obj, .coef.col = "Infection")

# Basic heatmap using nmfDE1
plotNmfDEHeatmap(lm.obj, .coef.col = "Infection", .nmfDE.dim = 1)

# Order by two dimensions (e.g., to see 2D structure)
plotNmfDEHeatmap(lm.obj, .coef.col = "Infection", .nmfDE.dim = c(1, 2))

# Use mass loadings instead of DE loadings
plotNmfDEHeatmap(lm.obj, .coef.col = "Infection", .nmfDE.dim = 1,
                 .loadings.type = "mass")

# Order by nmfDE dimension scores
plotNmfDEHeatmap(lm.obj, .coef.col = "Infection", .nmfDE.dim = 1,
                 .order.by = "nmfDE.dim")
}

}
\seealso{
\code{\link{get.nmfDE}} for computing nmfDE, \code{\link{plotNmfDE}}
for score visualization
}
