% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/h5ad_utils.R
\name{subset_h5ad}
\alias{subset_h5ad}
\title{Subset and reorder an on-disk h5ad file without full materialization}
\usage{
subset_h5ad(
  .source.path,
  .dest.path,
  .cell.idx,
  .gene.idx = NULL,
  .layers = NULL,
  .copy.obsm = TRUE,
  .copy.obsp = FALSE,
  .copy.varm = TRUE,
  .copy.varp = FALSE,
  .copy.uns = TRUE,
  .chunk.size = 5000L,
  .compression = 0L,
  .verbose = TRUE
)
}
\arguments{
\item{.source.path}{Character scalar. Path to the source .h5ad file.}

\item{.dest.path}{Character scalar. Path for the output .h5ad file.
Must differ from \code{.source.path}.}

\item{.cell.idx}{Integer vector. 1-based row (cell) indices to keep,
in the desired output order. Must be valid indices into the source obs.}

\item{.gene.idx}{Integer vector or \code{NULL}. 1-based column (gene)
indices to keep, in the desired output order. \code{NULL} keeps all genes
in original order.}

\item{.layers}{Character vector or \code{NULL}. Names of additional layers
(under \code{/layers/}) to copy. \code{NULL} copies all layers found.
Use \code{character(0)} to skip layers entirely.}

\item{.copy.obsm}{Logical. Whether to copy \code{/obsm} (cell embeddings).
Default \code{TRUE}.}

\item{.copy.obsp}{Logical. Whether to copy \code{/obsp} (cell-cell graphs).
Default \code{FALSE} (these are large and rarely needed after subsetting).}

\item{.copy.varm}{Logical. Whether to copy \code{/varm} (gene loadings).
Default \code{TRUE}.}

\item{.copy.varp}{Logical. Whether to copy \code{/varp}. Default \code{FALSE}.}

\item{.copy.uns}{Logical. Whether to copy \code{/uns} (unstructured metadata).
Default \code{TRUE}.}

\item{.chunk.size}{Integer. Number of cells (rows) per chunk when copying
matrices. Controls peak memory. Default \code{5000L}.}

\item{.compression}{Integer 0-9. gzip compression level for output datasets.
\code{0L} = no compression (fastest). Default \code{0L}.}

\item{.verbose}{Logical. Print progress messages. Default \code{TRUE}.}
}
\value{
Invisible character scalar: the \code{.dest.path}.
}
\description{
Performs a chunked, streaming copy of an HDF5-backed .h5ad file,
subsetting and reordering cells (rows of X) and optionally genes
(columns of X) without ever loading the full matrix into memory.
}
\details{
Requires the \pkg{rhdf5} Bioconductor package.

\strong{Supported matrix encodings}: \code{"csr_matrix"}, \code{"csc_matrix"},
and \code{"array"} (dense). The encoding is detected from the
\code{encoding-type} HDF5 attribute on each matrix group/dataset.

\strong{Peak memory}: approximately \code{.chunk.size * n_genes_out} numeric
values, regardless of total dataset size.

\strong{Categorical columns}: obs/var columns stored as h5ad categoricals
(codes + categories) are correctly handled.
}
\examples{
\dontrun{
subset_h5ad(
  .source.path = "full_data.h5ad",
  .dest.path   = "subset.h5ad",
  .cell.idx    = which(metadata$keep),
  .gene.idx    = which(!duplicated(gene_symbols)),
  .chunk.size  = 5000L
)
h5ad <- anndataR::read_h5ad("subset.h5ad", as = "HDF5AnnData")
}

}
\seealso{
\code{\link[anndataR]{read_h5ad}} to open the result.
}
