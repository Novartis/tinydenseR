% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunTDR.R
\name{RunTDR.h5ad}
\alias{RunTDR.h5ad}
\title{Run the tinydenseR pipeline on an h5ad file}
\usage{
\method{RunTDR}{h5ad}(
  x,
  .sample.var,
  .meta,
  .assay.type = "RNA",
  .assay.layer = "X",
  .harmony.var = NULL,
  .markers = NULL,
  .celltype.vec = NULL,
  .min.cells.per.sample = 10,
  .optimize.hdf5 = FALSE,
  .verbose = TRUE,
  .seed = 123,
  .prop.landmarks = 0.1,
  .n.threads = if (is.hpc()) {
     max(RhpcBLASctl::blas_get_num_procs(),
    RhpcBLASctl::omp_get_num_procs(), RhpcBLASctl::omp_get_max_threads(), na.rm = TRUE)

    } else {
     parallel::detectCores(logical = TRUE)
 },
  ...
)
}
\arguments{
\item{x}{Character(1). Path to the \code{.h5ad} file.}

\item{.sample.var}{Character(1). Column in \code{adata$obs} identifying
sample membership.}

\item{.meta}{A data.frame of sample-level metadata. Row names must
correspond to sample IDs in \code{adata$obs[[.sample.var]]}.}

\item{.assay.type}{Character. \code{"RNA"} or \code{"cyto"}.}

\item{.assay.layer}{Character(1). Layer name in the AnnData object
(default \code{"X"}).}

\item{.harmony.var}{Character vector of batch variable column names
in \code{.meta}, or \code{NULL}.}

\item{.markers}{Character vector of marker names (required for cyto).}

\item{.celltype.vec}{Character(1). Column name in \code{adata$obs}
containing per-cell type labels, or \code{NULL}.}

\item{.min.cells.per.sample}{Integer. Minimum cells for a sample to be
included.}

\item{.optimize.hdf5}{Logical. If \code{TRUE}, warns that h5ad files
cannot be reordered in-place and suggests conversion to SCE.}

\item{.verbose}{Logical. Print progress messages.}

\item{.seed}{Integer. Random seed.}

\item{.prop.landmarks}{Numeric in (0, 1]. Proportion of cells as landmarks.}

\item{.n.threads}{Integer. Number of threads.}

\item{...}{Additional arguments passed to pipeline functions.}
}
\value{
A \code{\linkS4class{TDRObj}} (bare object — no container to
store it in).
}
\description{
Reads an \code{.h5ad} file via \pkg{anndataR}, builds a
\code{\linkS4class{TDRObj}}, and executes the full pipeline.
}
\details{
This is a standalone exported function, not an S3 method (the
\code{"character"} class is too generic for dispatch).
}
