% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate_DE_data}
\alias{simulate_DE_data}
\title{Simulate differential expression (DE) flow cytometry data}
\usage{
simulate_DE_data(
  groups = c("Baseline", "Activation"),
  batches = c("Batch1", "Batch2"),
  sd_shifts = c(0.5, 1, 2),
  samples_per_group = 6,
  mean_cells = 50000,
  sd_cells = 500,
  seed = 42,
  output_dir = file.path(tempdir(), "sim_DE_fcs")
)
}
\arguments{
\item{groups}{Character vector of treatment group names.}

\item{batches}{Character vector of batch names.}

\item{sd_shifts}{Numeric vector of standard deviation shifts applied
to Marker2 in the non-Baseline group for target cells.}

\item{samples_per_group}{Integer. Number of samples per group per shift.}

\item{mean_cells}{Integer. Mean number of cells per sample.}

\item{sd_cells}{Numeric. Standard deviation of cell count.}

\item{seed}{Integer. Random seed for reproducibility.}

\item{output_dir}{Character. Directory path where FCS files are written.}
}
\value{
A list with two elements:
\describe{
\item{sample_meta}{A \code{data.frame} with one row per sample
and columns \code{Sample}, \code{Treatment}, \code{Batch},
\code{SD_Shift}, \code{fcs_path}.}
\item{cell_meta}{A \code{data.frame} with one row per cell
and columns \code{Sample}, \code{Treatment}, \code{Batch},
\code{SD_Shift}, \code{CellType}.}
}
}
\description{
Generates simulated flow cytometry data with differential expression
of Marker2 in a target cell type across treatment groups, batch effects,
and multiple shift magnitudes. Writes per-sample FCS files and returns
sample- and cell-level metadata.
}
\examples{
\dontrun{
sim <- simulate_DE_data(
  samples_per_group = 2,
  mean_cells = 1000,
  sd_cells = 100,
  output_dir = file.path(tempdir(), "sim_DE_fcs")
)
head(sim$sample_meta)
head(sim$cell_meta)
}

}
