% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plotMarkerDE}
\alias{plotMarkerDE}
\title{Plot Marker DE Results}
\usage{
plotMarkerDE(x, ...)
}
\arguments{
\item{x}{A TDRObj, Seurat, or SingleCellExperiment object.}

\item{...}{Additional arguments passed to the TDRObj method.}

\item{.tdr.obj}{A tinydenseR object with marker results from \code{get.markerDE}.}

\item{.de.obj}{Optional: Direct DE results object for backward compatibility. If NULL, fetches
from \code{.tdr.obj$markerDE[[.model.name]][[.comparison.name]]}.}

\item{.model.name}{Character identifying the model (default "default").}

\item{.comparison.name}{Character identifying the comparison (e.g., "cluster.1_vs_all"). Required
if \code{.de.obj} is NULL.}

\item{.coefs}{Character vector of coefficient names to plot. Default includes "(Intercept)" and
".id1" to show overall expression and group-specific enrichment.}

\item{.order.by}{Source for marker ordering: "clustering" or "celltyping".}

\item{.markers}{Features to include (default: all markers from \code{.order.by}).}

\item{.q}{Numeric FDR threshold for significance markers (default 0.1).}

\item{.row.space.scaler}{Numeric row height scaling (default 0.2).}

\item{.col.space.scaler}{Numeric column width scaling (default 0.065).}

\item{.label.substr.rm}{Character pattern to remove from coefficient labels.}
}
\value{
A \code{ggplot} heatmap showing log fold changes per feature and coefficient.
}
\description{
Visualizes marker identification results from \code{get.markerDE} as a colored heatmap showing
log fold changes with significance overlays. Uses the new storage pattern where results are
stored in \code{.tdr.obj$markerDE[[.model.name]][[.comparison.name]]}.
}
\details{
The plot shows:
\itemize{
\item Fill color: Log fold change (blue = lower in .id1, red = higher in .id1)
\item Point size: -log10(adjusted p-value)
\item Asterisk overlay: Features passing the q-value threshold
}

For marker analysis, the key coefficient is typically ".id1" which represents the difference
between group 1 and group 2 (or all other landmarks).
}
\examples{
\dontrun{
# After running get.markerDE
lm.cells <- get.markerDE(lm.cells, .id1 = "cluster.3", 
                         .comparison.name = "cluster3_markers")

# Visualize marker results
plotMarkerDE(lm.cells, .comparison.name = "cluster3_markers",
             .coefs = c("(Intercept)", ".id1"))
}

}
\seealso{
\code{\link{get.markerDE}}, \code{\link{plotPbDE}}
}
