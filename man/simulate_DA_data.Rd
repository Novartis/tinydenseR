% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate_DA_data}
\alias{simulate_DA_data}
\title{Simulate differential abundance (DA) flow cytometry data}
\usage{
simulate_DA_data(
  groups = c("Baseline", "Depletion"),
  batches = c("Batch1", "Batch2"),
  settings = c(0.005, 0.05, 0.5),
  samples_per_group = 6,
  mean_cells = 50000,
  sd_cells = 500,
  seed = 42,
  output_dir = file.path(tempdir(), "sim_DA_fcs")
)
}
\arguments{
\item{groups}{Character vector of treatment group names.}

\item{batches}{Character vector of batch names.}

\item{settings}{Numeric vector of proportions for the Baseline group.
The non-Baseline group uses half the proportion.}

\item{samples_per_group}{Integer. Number of samples per group per setting.}

\item{mean_cells}{Integer. Mean number of cells per sample.}

\item{sd_cells}{Numeric. Standard deviation of cell count.}

\item{seed}{Integer. Random seed for reproducibility.}

\item{output_dir}{Character. Directory path where FCS files are written.}
}
\value{
A list with two elements:
\describe{
\item{sample_meta}{A \code{data.frame} with one row per sample
and columns \code{Sample}, \code{Treatment}, \code{Batch},
\code{Setting}, \code{fcs_path}.}
\item{cell_meta}{A \code{data.frame} with one row per cell
and columns \code{Sample}, \code{Treatment}, \code{Batch},
\code{Setting}, \code{CellType}.}
}
}
\description{
Generates simulated flow cytometry data with differential abundance of a
target cell type across treatment groups, batch effects, and multiple
abundance settings. Writes per-sample FCS files and returns sample- and
cell-level metadata.
}
\examples{
\dontrun{
sim <- simulate_DA_data(
  samples_per_group = 2,
  mean_cells = 1000,
  sd_cells = 100,
  output_dir = file.path(tempdir(), "sim_DA_fcs")
)
head(sim$sample_meta)
head(sim$cell_meta)
}

}
