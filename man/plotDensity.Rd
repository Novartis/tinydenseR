% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plotDensity}
\alias{plotDensity}
\alias{plotDensity.TDRObj}
\title{Plot Density}
\usage{
plotDensity(x, ...)

\method{plotDensity}{TDRObj}(
  x,
  .x.split = colnames(x = .tdr.obj@metadata)[1],
  .pop = NULL,
  .pop.from = "clustering",
  .subject.id = NULL,
  .color.by = NULL,
  .x.space.scaler = 0.25,
  .height = 1.5,
  .cat.feature.color = Color.Palette[1, 1:5],
  .seed = 123,
  .orientation = "wide",
  ...
)
}
\arguments{
\item{x}{A \code{\linkS4class{TDRObj}}, Seurat, SingleCellExperiment, or HDF5AnnData
(anndataR) object processed through \code{get.map()}.}

\item{...}{Additional arguments passed to methods.}

\item{.x.split}{Character specifying metadata column for x-axis grouping (default first column).}

\item{.pop}{Character vector of population names to plot. If NULL, plots all landmarks. If
specified, plots only landmarks belonging to that population (from \code{.pop.from}).}

\item{.pop.from}{Character: "clustering" (default) or "celltyping" - which grouping to use for
filtering landmarks when \code{.pop} is specified.}

\item{.subject.id}{Character metadata column for connecting paired samples with lines (e.g.,
"Subject"). Default NULL.}

\item{.color.by}{Character metadata column for coloring points. Default NULL (all black).}

\item{.x.space.scaler}{Numeric x-axis width scaling (default 0.25 inches per group).}

\item{.height}{Numeric plot height in inches (default 1.5).}

\item{.cat.feature.color}{Character vector of colors (default \code{Color.Palette[1,1:5]}).}

\item{.seed}{Integer random seed for jitter (default 123).}

\item{.orientation}{Character: "wide" (default) or "square" faceting.}
}
\value{
A \code{ggplot} object showing log2-transformed landmark densities.
}
\description{
Creates dot/line plots showing log2-transformed landmark fuzzy densities per sample.
Unlike \code{plotTradPerc()} which plots cell percentages at the cluster/celltype level, this
function plots landmark-level densities Useful for inspecting distributions and
paired/longitudinal patterns at the landmark resolution.
}
\examples{
\dontrun{
# Basic density plot
plotDensity(lm.cells, .pop = "B.cells")

# With paired subject lines
plotDensity(lm.cells, 
              .pop = "B.cells",
              .subject.id = "MouseID",
              .color.by = "Treatment")
}

}
\seealso{
\code{\link{plotTradPerc}}, \code{\link{plotTradStats}}
}
