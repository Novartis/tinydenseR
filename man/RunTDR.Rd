% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RunTDR.R
\name{RunTDR}
\alias{RunTDR}
\alias{RunTDR.default}
\alias{RunTDR.Seurat}
\alias{RunTDR.SingleCellExperiment}
\alias{RunTDR.HDF5AnnData}
\alias{RunTDR.dgCMatrix}
\alias{RunTDR.DelayedMatrix}
\alias{RunTDR.IterableMatrix}
\title{Run the full tinydenseR pipeline}
\usage{
RunTDR(x, ...)

\method{RunTDR}{default}(
  x,
  .celltype.vec = NULL,
  .celltype.vec.overwrite = FALSE,
  .verbose = TRUE,
  .seed = 123,
  ...
)

\method{RunTDR}{Seurat}(
  x,
  .sample.var,
  .assay = "RNA",
  .layer = "counts",
  .assay.type = "RNA",
  .harmony.var = NULL,
  .markers = NULL,
  .celltype.vec = NULL,
  .min.cells.per.sample = 10,
  .verbose = TRUE,
  .seed = 123,
  .prop.landmarks = 0.1,
  .n.threads = if (is.hpc()) {
     max(RhpcBLASctl::blas_get_num_procs(),
    RhpcBLASctl::omp_get_num_procs(), RhpcBLASctl::omp_get_max_threads(), na.rm = TRUE)

    } else {
     parallel::detectCores(logical = TRUE)
 },
  ...
)

\method{RunTDR}{SingleCellExperiment}(
  x,
  .sample.var,
  .assay = "counts",
  .assay.type = "RNA",
  .harmony.var = NULL,
  .markers = NULL,
  .celltype.vec = NULL,
  .min.cells.per.sample = 10,
  .optimize.hdf5 = FALSE,
  .verbose = TRUE,
  .seed = 123,
  .prop.landmarks = 0.1,
  .n.threads = if (is.hpc()) {
     max(RhpcBLASctl::blas_get_num_procs(),
    RhpcBLASctl::omp_get_num_procs(), RhpcBLASctl::omp_get_max_threads(), na.rm = TRUE)

    } else {
     parallel::detectCores(logical = TRUE)
 },
  ...
)

\method{RunTDR}{HDF5AnnData}(
  x,
  .sample.var,
  .assay.type = "RNA",
  .assay.layer = "counts",
  .harmony.var = NULL,
  .markers = NULL,
  .celltype.vec = NULL,
  .min.cells.per.sample = 10,
  .optimize.hdf5 = FALSE,
  .verbose = TRUE,
  .seed = 123,
  .prop.landmarks = 0.1,
  .n.threads = if (is.hpc()) {
     max(RhpcBLASctl::blas_get_num_procs(),
    RhpcBLASctl::omp_get_num_procs(), RhpcBLASctl::omp_get_max_threads(), na.rm = TRUE)

    } else {
     parallel::detectCores(logical = TRUE)
 },
  ...
)

\method{RunTDR}{dgCMatrix}(x, .cell.meta, ...)

\method{RunTDR}{DelayedMatrix}(x, .cell.meta, ...)

\method{RunTDR}{IterableMatrix}(x, .cell.meta, ...)
}
\arguments{
\item{x}{A \code{dgCMatrix} (features x cells for RNA,
cells x features for cyto).}

\item{...}{Additional arguments passed to pipeline functions.}

\item{.celltype.vec}{Character(1). Column name in \code{.cell.meta}
containing per-cell type labels, or \code{NULL}.}

\item{.celltype.vec.overwrite}{Logical. If \code{TRUE}, replace an
existing \code{.celltype.vec} stored in the TDRObj config.}

\item{.verbose}{Logical. Print progress messages.}

\item{.seed}{Integer. Random seed.}

\item{.sample.var}{Character(1). Column name in \code{.cell.meta}
identifying sample membership.}

\item{.assay}{Character(1). Name of the assay in \code{assayNames(x)}.}

\item{.layer}{Character(1). Layer within the assay (e.g. \code{"counts"}).}

\item{.assay.type}{Character. \code{"RNA"} or \code{"cyto"}.}

\item{.harmony.var}{Character vector of batch variable column names
in sample-level metadata, or \code{NULL}.}

\item{.markers}{Character vector of marker names (required for cyto).}

\item{.min.cells.per.sample}{Integer. Minimum cells for a sample to be
included.}

\item{.prop.landmarks}{Numeric in (0, 1]. Proportion of cells as landmarks.}

\item{.n.threads}{Integer. Number of threads.}

\item{.optimize.hdf5}{Logical. If \code{TRUE} and the assay is
HDF5-backed with poor contiguity, reorder the SCE columns by sample
for faster I/O.}

\item{.assay.layer}{Character(1). Layer name in the AnnData object
(default \code{"counts"}).}

\item{.cell.meta}{A \code{data.frame} of per-cell metadata.
Must have one row per cell with rownames matching cell IDs
in \code{x} (\code{colnames} for RNA, \code{rownames} for cyto).}
}
\value{
Depends on the method; see individual method documentation.

The updated \code{\linkS4class{TDRObj}}.

The Seurat object \code{x} with the TDRObj stored in
\code{Misc(x, slot = "tdr.obj")}.

The \code{SingleCellExperiment} \code{x} with the TDRObj stored
in \code{S4Vectors::metadata(x)$tdr.obj}.

A \code{\linkS4class{TDRObj}} (bare object — no container to
store it in).

A \code{\linkS4class{TDRObj}}.
}
\description{
S3 generic that executes the complete tinydenseR analysis pipeline:
landmark selection, graph construction, optional cell typing, and mapping.
}
\section{Methods (by class)}{
\itemize{
\item \code{RunTDR(default)}: Run the pipeline on an existing TDRObj

Executes \code{get.landmarks → get.graph → celltyping → get.map}
on a pre-built \code{\linkS4class{TDRObj}}.

\item \code{RunTDR(Seurat)}: Run the pipeline directly on a Seurat object

Builds a \code{\linkS4class{TDRObj}} from a Seurat object and executes
the full pipeline. The finished TDRObj is stored in
\code{SeuratObject::Misc(x, slot = "tdr.obj")}.

\item \code{RunTDR(SingleCellExperiment)}: Run the pipeline directly on a SingleCellExperiment

Builds a \code{\linkS4class{TDRObj}} from a \code{SingleCellExperiment}
and executes the full pipeline. The finished TDRObj is stored in
\code{S4Vectors::metadata(x)$tdr.obj}.

\item \code{RunTDR(HDF5AnnData)}: Run the pipeline on an HDF5AnnData object

Builds a \code{\linkS4class{TDRObj}} from an HDF5-backed AnnData
object and executes the full pipeline.

\item \code{RunTDR(dgCMatrix)}: Run the pipeline on a sparse matrix (dgCMatrix)

Builds a \code{\linkS4class{TDRObj}} from a \code{dgCMatrix} and
per-cell metadata, then executes the full pipeline.

\item \code{RunTDR(DelayedMatrix)}: Run the pipeline on a DelayedMatrix

\item \code{RunTDR(IterableMatrix)}: Run the pipeline on a BPCells IterableMatrix

}}
